#include <stdio.h>
#include "01demo.h"
void one_demo(){
    // int a;
    // a =32;
    // printf("a = %d, a is sizeof = %lu \n", a, sizeof(a));

    // int n = 0x11223344;
    // char *p = &n;
    // printf("%x\n",*p);
    

}

void two_demo(){

    // int a[5] = {1,2,3,4,5};

    // printf("%zu\n",sizeof(a));
    // printf("%zu \n",sizeof(a+1));
    // printf("%zu \n",sizeof(a[1]));

    // char * a[4] = {"hello","world","rust","python"};
    // char ** add = a;
    // add++;
    // printf("%s",*(add));
    //   printf("%zu",*(add));
    int a = 200;
printf("a = %d\n", a);
if ( a <10){
     if (!12) {
        printf("100");
     }
}
else{
    printf("11111");
}
}